#ifndef H_UTILS
#define H_UTILS

#include <sys/time.h>

/**
 * Time in nano seconds (High Resolution Time)
 */
uint64_t getHRTime();

/**
 * Time in seconds (High Resolution Time)
 */
float getHRTimeF();

#endif // !H_UTILS
